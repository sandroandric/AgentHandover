//! Focus Recording Mode — re-exports from common crate.
//!
//! The core types and functions live in `oc_apprentice_common::focus_session`
//! so they can be shared between the daemon, CLI, and worker.

pub use oc_apprentice_common::focus_session::*;
